import React from "react";

import { Button } from "../reusable/Button";

const Nav = () => {
  return (
    <div className="nav">
      <Button>About</Button>
      <Button>Users</Button>
    </div>
  );
};

export default Nav;
